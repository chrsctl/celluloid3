[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "celluloid3-core"
version = "0.1.0"
description = "Native scoring kernel for celluloid3"
requires-python = ">=3.10"
license = { text = "MIT" }

[tool.maturin]
module-name = "celluloid3_core"
